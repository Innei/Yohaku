import type { SiteOverlay } from '@/site-config'

/** Public-tree stub. Metro remaps `yohaku-mobile-overlay` to a closed overlay when that sibling directory exists. */
export const siteOverlay: SiteOverlay | null = null
